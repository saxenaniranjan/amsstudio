# Optional: explicit API host for deployed frontend.
# Leave unset to call same-origin /api (recommended for Vercel).
VITE_API_URL=

# Optional local dev proxy target used by vite.config.js
VITE_DEV_API_PROXY=http://127.0.0.1:8010
